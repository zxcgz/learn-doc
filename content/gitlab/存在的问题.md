# 存在的问题

### submodules

在添加子模块的情况下，可能会存在clone失败，报

```shell
warning: 远程 HEAD 指向一个不存在的引用，无法检出。
```

错误的情况

可以进入clone的目录中的.git/refs目录中，创建一个remote目录，进入remote目录，创建一个master文件，在文件中写上要克隆项目的hash值，然后复制一份到remote同级的heads目录中

然后回到.git所在的目录中，执行git checkout，就可以将项目clone出来